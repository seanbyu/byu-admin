/**
 * @deprecated
 * 이 파일은 더 이상 사용되지 않습니다.
 * 대신 `@/features/bookings/stores` 를 사용하세요.
 *
 * import { useBookingsStore } from '@/features/bookings/stores';
 */

export { useBookingsUIStore as useBookingStore } from "@/features/bookings/stores";
